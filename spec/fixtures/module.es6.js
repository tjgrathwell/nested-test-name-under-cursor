import router from 'modules/router';
import storage from 'modules/storage';

describe('my module test suite', function () {
  it('evaluates the code', function () {
    expect(2 + 2).toEqual(4);
  })
});